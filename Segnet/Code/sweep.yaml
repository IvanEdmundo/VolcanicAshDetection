program: SegNet-Basic.py
method: grid
metric:
  name: val_acc
  goal: maximize
parameters:
  epochs:
    distribution: int_uniform
    min: 10
    max: 10000
  batch_size:
    distribution: int_uniform
    min: 1
    max: 3
  learning_rate:
    distribution: uniform
    min: 0.005
    max: 0.002
